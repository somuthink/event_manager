export interface NavItem {
    name: string;
    link: string;
    description: string;
}

export interface Event {
    title: string;
    description: string;
    theme: string;
}

export interface Combo {
    value: string;
    label: string;
}
